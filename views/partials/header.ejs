<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Student Hub</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav class="navbar">
    <div class="container nav-container">
      <a href="/" class="logo">🎓 Student Hub</a>
      <ul class="nav-menu">
        <li><a href="/">Home</a></li>
        <li><a href="/directory">Directory</a></li>
        <% if (user) { %>
          <% if (user.role === 'admin') { %>
            <li><a href="/admin/dashboard">Admin</a></li>
          <% } else { %>
            <li><a href="/profile/me">My Profile</a></li>
          <% } %>
          <li><a href="/auth/logout" class="btn-logout">Logout</a></li>
        <% } else { %>
          <li><a href="/auth/login" class="btn-primary">Login</a></li>
          <li><a href="/auth/signup" class="btn-secondary">Sign Up</a></li>
        <% } %>
      </ul>
      <button class="mobile-toggle" id="mobileToggle">☰</button>
    </div>
  </nav>

  <% if (success_msg) { %>
    <div class="alert alert-success">
      <%= success_msg %>
      <button class="close-alert">&times;</button>
    </div>
  <% } %>

  <% if (error_msg) { %>
    <div class="alert alert-error">
      <%= error_msg %>
      <button class="close-alert">&times;</button>
    </div>
  <% } %>

  <main class="main-content">